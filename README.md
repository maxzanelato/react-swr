# ReactJs + SWR + Typescript + Axios
Application to test SWR with Typescript.

## Components
- ReactJs;
- Typescript;
- Axios;
- json-server.

## How to run?
1. `yarn server` to run json server;
2. `yarn start` to run the app.
